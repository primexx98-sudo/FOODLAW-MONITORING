# 식품 법령 개정 모니터 설계서

## UI/UX 디벨롭 6차 — 버그 1건 + 개선 2건 (2026-08-27, 로컬 검증 완료)

"법령 모니터 개선하자"는 열린 요청에 AskUserQuestion으로 방향 확인 → "UI/UX 추가 디벨롭" 선택.
`build_site.py`를 코드 리뷰(브라우저 자동화 미연결이라 소스 분석으로 먼저 진단)해 구체적 버그 1건을
발견해 제시하자, 사용자가 "이 버그 고치고 + 이미 알고 있던 문제 2건도 같이"로 확정.

1. **"이전 주 접기" 버튼이 사실상 아무 동작도 안 함(발견 즉시 수정)**: `collapseAll()`이
   `.accordion-btn.open:not(:first-of-type)`으로 "최신 주차를 제외한 나머지"를 골라내려
   했으나, `:first-of-type`은 **부모마다** 개별 스코프로 평가되는 선택자 — 각 accordion-btn은
   자기 부모(`.week-section`)의 유일한 자식이라 전부 "자기 부모 안의 첫 번째"에 해당해
   `:not(:first-of-type)`이 아무것도 못 골라냄. 결과적으로 이 버튼을 눌러도 (선행 로직이 먼저
   모든 닫힌 아코디언을 열어버리는 탓에) 사실상 "모두 펼치기"와 똑같이 동작했음. DOM 순서
   인덱스(`forEach((b,i)=>...i===0...)`) 기반으로 재작성해 첫 번째(최신 주차)만 열어두고
   나머지는 명시적으로 닫도록 수정.
2. **월간 통합 요약 카드 5개월 제한**: 달이 쌓일수록(2023~2026, 44개월) 카드가 화면을 통째로
   차지하던 문제 — 표시사항 변경 추적에 이미 있던 "더보기" 패턴(`hidden` 속성 + extra 클래스)을
   그대로 재사용해 최근 `MONTHLY_VISIBLE_LIMIT`(5)개월만 기본 노출, 나머지는
   `toggleMonthlyMore()`로 펼침. `.month-section`엔 `display`를 선언하는 author 규칙이 없어
   `.spotlight-item`때와 달리 `[hidden]` 오버라이드 CSS 없이도 정상 작동함을 확인 후 진행.
3. **카드 폭 정렬 → 정정: 실제로는 2단 컬럼 배치 요청이었음**: 처음엔 "표시사항 변경 추적과
   주차 박스가 세로로 나열됐으면 좋겠다"를 "카드 폭을 주차 목록과 맞춰서 한 줄로 쌓기"로
   오해해 `.card`/`.cat-dropdown`에 `max-width:820px;margin:...auto`를 적용했으나(주차 목록
   `.main`의 내부 콘텐츠 폭과 일치시킨 것), 사용자가 "변경추적은 왼쪽 아래로 쭉, 주차요약은
   오른쪽 아래로 쭉"이라고 재설명 — **왼쪽 컬럼에 표시사항 변경 추적이, 오른쪽 컬럼에 주차
   아코디언 목록이 각각 독립적으로 세로로 쌓이는 2단 그리드**를 원한 것이었음. `.content-grid`
   (`grid-template-columns:320px 1fr`)를 신설해 `label_spotlight_html`을 `.content-left`,
   `weeksMain`을 `.content-right`로 감쌈(월간 통합 요약 카드는 그대로 상단 전체폭 유지, 이
   2단 그리드 대상이 아님). 1080px 이하에서는 `grid-template-columns:1fr`로 단일 컬럼 폴백.
   **발견 버그**: 왼쪽 컬럼이 뷰포트 폭과 무관하게 항상 320px 고정이라, `.spotlight-item`의
   기존 가로 flex 배치(태그+제목+날짜 한 줄)가 375px 모바일에서 겪었던 것과 동일하게 제목이
   심하게 줄바꿈됨 — 미디어쿼리 대신 `.content-left .spotlight-item`을 스코프로 삼아 뷰포트
   폭과 무관하게 태그·날짜는 윗줄, 제목은 아랫줄 전체너비로 항상 분리하도록 수정.
- **검증**: 로컬 `python -m http.server`+Playwright로 (1) 데스크톱 1440px에서 카드-주차 박스
  좌우 정렬 일치, (2) 월간 요약 5개월 노출 + "더보기" 클릭 시 39개월 전부 펼쳐짐 + 버튼 텍스트
  전환, (3) 모바일 390px에서도 동일 폭 정렬(12px 여백)·5개월 제한 정상 확인, (4) "모두 펼치기" →
  "이전 주 접기" 클릭 시 35개 열림 상태에서 1개(최신 주차)만 남고 전부 닫힘, (5) 콘솔 에러 0건
  — 전부 실제 브라우저(headless Chromium)로 확인. **주의**: 로컬 검증만 완료, 커밋·push는 보류
  (기존 합의 — 사용자 검토 후 진행).
**Why**: 브라우저 자동화 도구(claude-in-chrome)가 이 세션엔 연결 안 돼 있어, "실제로 클릭해보고
진단"하던 기존 패턴 대신 소스 코드를 먼저 정독해 버그를 찾아낸 뒤 로컬 Playwright로 검증하는
순서로 진행 — 결과적으로 소스 리뷰만으로도 실제 동작하지 않는 버튼을 정확히 짚어낼 수 있었음.
**How to apply**: 이 프로젝트에서 `hidden` 속성으로 토글하는 새 요소를 추가할 때는, 그 선택자에
`display`를 선언하는 author 규칙이 이미 있는지 먼저 확인할 것(있으면 `[hidden]` 오버라이드 CSS를
추가해야 하고, 없으면 불필요 — `.month-section`은 후자라 추가 CSS 없이 그대로 동작함).
`.card`류의 새 카드 컴포넌트를 추가할 때는 반드시 `max-width:820px;margin:...auto`를 함께
지정해 `.main`(주차 목록)과 폭을 맞출 것 — 안 그러면 이번처럼 넓은 화면에서만 드러나는 폭
불일치가 재발한다.

## 전면 리뉴얼 — 사이드바 내비게이션형 (2026-08-26, 배포 완료)

**상태**: 로컬에서 먼저 검토(스크린샷·인터랙션 확인) 후 사용자가 "이대로 바꾸자"고 확정해
커밋·푸시 및 GitHub Pages 배포 완료.

**배경**: "UI/UX 색만 바꾼 게 아니라 배치도 갈아엎고 싶다"는 요청으로 Stripe·Linear·ClickHouse
디자인 언어를 리서치 → 실제 콘텐츠로 3가지 후보(사이드바 내비게이션형/커맨드바+테이블형/벤토
그리드형)를 Artifact로 만들어 비교 → **사이드바 내비게이션형**을 선택받아 구현.

**핵심 변경**:
- **정보 구조**: 연도·상태·유형(카테고리) 필터를 전부 왼쪽 고정 사이드바(`<aside class="sidebar">`)
  세로 목록으로 이동 — 예전엔 툴바(검색)·필터바(연도/상태)·유형별 현황 그리드(카테고리)가 화면
  세 군데에 흩어져 있었음. `render_category_nav()`(구 `render_category_overview`)가 그리드 카드
  대신 세로 nav-item 목록을 생성 — `data-cat-btn` 속성은 그대로라 `setCategory`/
  `renderCatDropdown` JS는 변경 없이 재사용됨(카테고리 클릭 시 결과는 본문 상단 `#catDropdown`에
  표시, 위치만 사이드바 밖 본문으로 이동).
- **KPI 박스 → 한 줄 통계**: `.kpi-row`(4개 박스)를 제거하고 본문 상단 `.stat-strip`(텍스트 한 줄)로
  축소.
- **색 전면 교체**: 다크+옐로(Binance 계열) → **라이트+인디고**(Stripe 계열, `#5046e5`)가 기본
  테마로 바뀜. **기본값이 라이트로 반전**됐으므로(예전엔 다크가 기본, `[data-theme="light"]`가
  오버라이드였는데 이제 반대) 테마 토글 JS(`applyThemeIcon`/`toggleTheme`)도 "속성 없음=다크"에서
  "속성 없음=라이트"로 판정 기준을 같이 뒤집었음 — 안 그러면 첫 로드 시 아이콘이 실제 테마와
  반대로 표시되는 버그가 생김. 다크모드는 `[data-theme="dark"]`로 유지, 인디고 계열로 톤만 맞춤
  (`#7c74f5`).
- **연동된 리터럴 색상 값**: `--primary`를 노랑→인디고로 바꿔도 `rgba(252,213,53,X)`처럼 토큰이
  아니라 직접 박아넣은 rgba 값(예고 태그, 표시기준 카테고리 칩, 원문 바로가기 버튼, week/month
  count 배지, 딥링크 하이라이트 애니메이션 등 9곳)은 자동으로 안 바뀌므로 전부
  `rgba(80,70,229,X)`로 일괄 치환.
- **모바일**: 760px 이하에서 사이드바가 상단 가로 스트립으로 접힘(`.nav-group`에
  `flex-basis:100%`를 줘서 연도/상태/유형 그룹이 서로 줄이 안 섞이게 함 — 처음엔 이걸 빠뜨려서
  그룹끼리 한 줄에 섞이는 버그가 있었고, 개별 nav-item도 `width:100%`를 뺀 상태에서
  `justify-content:space-between`만으로는 라벨-카운트 간격이 안 생겨 `gap:6px`을 추가해서 고침).

**검증**: Playwright로 데스크톱/모바일/라이트/다크 스크린샷 + 콘솔 에러 0건 확인, 카테고리 클릭 →
드롭다운 정상 렌더(예: "기준·규격" 10건), 검색 → 사이드바 유지된 채 본문만 전환, 연도 필터 그룹이
검색 중 숨겨지는 것까지 실제 브라우저로 확인 완료.

### 후속 조정 (같은 날)
- **월간 통합 요약을 최상단으로**: `#normalView` 안에서 `monthly_summary_html`을 `.stat-strip`보다
  위, 즉 본문 맨 처음으로 이동(사용자 요청 — "요약 먼저 보고 싶다").
- **표시사항 변경 추적 5건 + 더보기**: 12건을 전부 펼쳐두면 카드 하나가 화면을 너무 차지한다는
  피드백 → 처음 `SPOTLIGHT_VISIBLE_LIMIT`(5)건만 보이고 나머지는 `.spotlight-extra` +
  `hidden` 속성으로 숨긴 뒤 "나머지 N건 더보기" 버튼(`toggleSpotlightMore`)으로 펼침.
  **버그 트랩**: `.spotlight-item{{display:flex}}`처럼 author 스타일시트가 이미 `display`를
  선언한 요소는 `[hidden]`(UA 스타일시트, `display:none`)이 origin 우선순위상 밀려서 절대
  안 먹힘 — 인라인 `style`이 아니라 평범한 클래스 규칙이어도 발생하는, 이 프로젝트에서 이미
  겪었던 "hidden vs author display" 함정의 변종. `.spotlight-item[hidden]{{display:none;}}`처럼
  속성 선택자를 명시적으로 더 얹어야 실제로 숨겨짐 — 앞으로 `display`를 이미 선언한 요소에
  `hidden` 속성으로 토글을 걸 때는 항상 이 패턴을 먼저 추가할 것.
- **Gemini 무료 티어 일일 20회 한도 재확인**: 월간 요약 배치가 최우선순위(당월)로 설계돼 있음에도
  당월 요약이 계속 안 뜨길래 로그를 보니, 그날의 첫 실행에서 일일 20회(분당 한도가 아니라
  하루 총량, `GenerateRequestsPerDayPerProjectPerModel-FreeTier`)를 이미 다 써버려 재시도해도
  429만 반환됨을 확인 — 당일 재트리거는 무의미, 다음날 자동 백필을 기다리는 수밖에 없음.

## 프로젝트 개요
- **목적**: 식약처의 식품 법령 개정 공고를 매주 자동 수집·아카이브
- **배포 URL**: https://primexx98-sudo.github.io/FOODLAW-MONITORING/
- **접근 권한**: 팀 내부 전용 (추후 Cloudflare Access 적용 예정)
- **2026-07-22**: 법제처 연동 제외 결정(아래 "데이터 소스 변경 이력" 참고) — 식약처 자체 게시판(법/시행령/시행규칙 + 입법·행정예고)만으로 충분하다고 판단

## 폴더 구조

```
FOODLAW-MONITORING/
├── .github/workflows/
│   ├── collect.yml                 # 매주 월요일 자동 실행 (신규 수집 + 요약 + 빌드)
│   └── backfill_summarize.yml      # 2026-07-23 신설: 매일 자동 실행, 과거 백로그 요약을 하루 30건씩 처리
├── scripts/
│   ├── collect_lawgokr.py          # 법제처 OpenAPI 수집 (2026-07-22부로 미사용, 파일만 보존)
│   ├── collect_mfds.py             # 식약처 게시판 크롤링 (m_203 법/시행령/시행규칙 + m_209 입법/행정예고, 식품 카테고리 필터)
│   ├── collect_foodsafety.py       # 식품안전나라 (2026-07-22부로 게시판 URL 무효화, 빈 리스트 반환하도록 비활성화)
│   ├── law_type_utils.py           # 식품 관련성 판별 + 법/시행령/시행규칙/행정예고 분류 (collect_mfds/foodsafety 공유)
│   ├── collect_all.py              # 전체 수집 진입점 (식약처+식품안전나라 호출 + archive.json 갱신)
│   ├── collect_historical.py       # 2026년 과거 주차 일괄 백필 (Playwright, 1회성 수동 실행, law.go.kr 기반이라 지금은 사실상 폐기)
│   ├── collect_historical_mfds.py  # 2026-07-23 신설: 식약처 게시판을 2023-01-01까지 페이지네이션으로 백필 (1회성)
│   ├── backfill_body_text.py       # 2026-07-23 신설: 기존 항목에 상세페이지 본문(body_text) 백필 (1회성)
│   ├── summarize_items.py          # Groq API(무료 티어)로 항목별 핵심내용·업계영향·주간요약·월간요약 생성 (최근 우선 + 과거 하루 30건 제한)
│   ├── category_utils.py           # 항목 내용 성격 분류(표시기준/기준규격/안전관리/수입수출/영업인증/기타), category_override 지원
│   └── build_site.py               # data/archive.json → docs/index.html(+ docs/data/archive-weeks.html) 빌드
├── data/
│   └── archive.json                # 주차별 누적 아카이브 데이터 (2023~2026년, 연도+주차 오름차순 저장) + months(월간요약)
├── docs/
│   ├── index.html                  # GitHub Pages 배포 파일 (자동 생성, 최신 연도만 인라인)
│   └── data/
│       └── archive-weeks.html      # 2026-08-26 신설: 최신 연도를 제외한 과거 주차 HTML 조각(자동 생성) — index.html이 필요할 때만 fetch
├── requirements.txt
├── 설계서.md
└── README.md
```

## 수집 출처

| 출처 | 방식 | API 키 |
|---|---|---|
| ~~법제처~~ | ~~국가법령정보 OpenAPI~~ | 2026-07-22 제외 (아래 "데이터 소스 변경 이력" 참고) |
| 식약처 | 게시판 HTML 크롤링 (m_203, m_209 — `data_stts_gubun=C1002`로 식품 카테고리만) | 불필요 |
| 식품안전나라 | 게시판 URL 무효화로 2026-07-22부터 비활성화(빈 리스트 반환) | 불필요 |
| Groq API (summarize_items.py) | 항목 요약 생성, 모델 `openai/gpt-oss-120b` | 필요 (GitHub Secret: GROQ_API_KEY, 무료 티어로 발급 가능 — https://console.groq.com/keys) |

## 자동화 흐름

```
매주 월요일 09:27 (KST) — 2026-07-23 변경 (기존 09:00 정각은 GitHub Actions 큐잉 지연이 심해 27분으로 조정)
→ GitHub Actions 트리거
→ collect_all.py 실행
  → 식약처 페이지 파싱 (m_203, m_209) — 목록 수집 + 상세페이지 본문(`fetch_detail_body`, 2026-07-23) 함께 수집
  → 식품안전나라 파싱 (현재 비활성화, 빈 리스트)
  → data/archive.json 업데이트 (연도+주차 오름차순 정렬 저장)
→ summarize_items.py 실행
  → key_points가 비어있거나 body_text가 새로 생긴 신규/기존 항목에 Groq API로 핵심내용·업계영향 생성 (실제 공고 본문을 근거로 사용, 2026-07-23)
  → 주차별 weekly_summary(통합 요약) 생성
→ build_site.py 실행
  → archive.json을 연도+주차 내림차순(최신 우선)으로 정렬해 docs/index.html 재생성
→ git commit & push
→ GitHub Pages 자동 배포
```

## 데이터 정렬 원칙

- `archive.json`에는 항상 **연도+주차 오름차순**(과거→최신)으로 저장한다 (`collect_all.py`가 저장 시 정렬).
- `build_site.py`는 렌더링 시 저장 순서와 무관하게 **연도+주차 내림차순**(최신→과거)으로 재정렬해 사이트 화면 맨 위에 최신 주차가 오도록 한다.
- 두 스크립트가 저장/조회 정렬 기준을 각각 독립적으로 보장하므로, 수동으로 archive.json을 편집해 순서가 뒤섞여도 사이트 표시는 항상 올바르다.

## UI 태그 규칙 (build_site.py)

- **관심 태그**: 제목 또는 law_type에 `건강기능식품`/`기능성`/`건기식` 키워드가 포함되면 자동으로 "관심" 태그 표시 (핵심 사업영역 강조).
- **전체 아카이브 복사**: 툴바의 "전체 아카이브 복사" 버튼은 각 주차의 weekly_summary(또는 summary)를 이어붙여 클립보드에 복사한다. `weeks` 전체를 서버에서 미리 문자열로 계산해두는 방식이라, 아래 "연도 지연 로딩"과 무관하게 항상 전체 기간을 포함한다.

## 표시사항 변경 추적 / 월간 통합 요약 카드 (2026-08-26)

- **표시사항 변경 추적**: `LABEL_SPOTLIGHT_CATEGORY = "표시기준"`으로 분류된 항목 중 최근 `LABEL_SPOTLIGHT_LIMIT`(12)건을 카테고리 현황 카드 아래에 별도 카드로 보여준다(`render_label_spotlight`). 각 항목 아래에 `industry_impact`(없으면 `key_points[0]`)를 2줄 요약으로 함께 표시.
- **월간 통합 요약**: `summarize_items.py`가 항목의 실제 날짜(`date[:7]`) 기준으로 월 단위 재집계해 Groq로 통합 요약문을 생성, `archive.json["months"]`(딕셔너리, 키=`YYYY-MM`)에 저장. 요약이 이미 있어도 그 달에 속한 항목 수가 바뀌면 재생성(주간 요약과 동일한 조건). `build_site.py`의 `render_monthly_summary`가 요약이 있는 달만 최신순 아코디언 카드로 렌더링(최신 달 기본 펼침).
- **월간 카드 ↔ 주간 목록 연결**: 각 월 카드 안 "이 달 항목 N건 보기" 버튼(`jumpToMonth(mk)`)을 누르면, 그 달에 속한 모든 주차(월 경계를 넘는 주차 포함 — `render_week`가 주차별 `data-months` 속성에 해당 주차 항목들의 YYYY-MM을 콤마로 나열해둠)를 자동으로 펼치고 첫 주차로 스크롤+하이라이트한다. 기존 `#law-xxxxx` 딥링크의 `deep-link-highlight` 애니메이션(`.law-item`용)을 `.week-section`에도 확장 적용해 재사용.

## 연도 지연 로딩 (2026-08-26)

**문제**: 연도가 쌓일수록 `docs/index.html`이 무한히 커짐(2026-08-26 기준 개선 전 1.4MB, 120주차·234건) — 가장 흔한 방문(이번 주만 확인)에서도 매번 전체 연도를 다운로드/파싱해야 했음.

**구조**: `build_site.py`가 `weeks`를 최신 연도(`default_year`)와 그 이전 연도로 나눠, 최신 연도만 `docs/index.html`에 인라인 렌더링하고 나머지는 `docs/data/archive-weeks.html`로 별도 저장한다. 클라이언트 JS `ensureAllYearsLoaded()`가 이 조각 파일을 메모이즈된 fetch로 가져와 `#weeksMain`에 append하며, 다음 4곳에서 실제 사용 직전에 `await`로 호출한다 — 나머지 로직(검색/필터/딥링크)은 그대로 유지:
- `setYear(btn, val)` — 최신 연도가 아닌 다른 연도/전체를 선택할 때
- `setSearch(val)` — 검색은 항상 전체 기간 대상
- `setCategory(btn, val)` — 카테고리 드롭다운이 연도 무관하게 전체 `.law-item`을 훑기 때문
- `handleDeepLink()` — health-trend에서 넘어오는 `#law-xxxxx`가 과거 연도를 가리킬 수 있음

**주의**: GitHub Pages는 커스텀 캐시 헤더를 지원하지 않아 "정적 파일이라 영구 캐싱됨" 같은 이득은 없다. 이번 개선의 실질 이득은 ①흔한 방문 경로(이번 주만 확인)에서 과거 데이터를 아예 안 받아도 됨 ②초기 HTML 파싱/렌더 비용 감소 두 가지로 한정된다.

## 카테고리 분류 수동 오버라이드 (2026-08-26)

`category_utils.categorize_item()`은 title/body_text 키워드 매칭만으로 매 빌드마다 전체 항목을 재분류하는 순수 함수라, 로직을 바꾸면 과거 항목에도 즉시 소급 적용되는 대신 개별 항목 하나만 손으로 고칠 방법이 없었다. `data/archive.json`의 해당 항목 객체에 `"category_override": "안전관리"`처럼 `CATEGORY_META`에 있는 카테고리 키를 직접 추가하면, 키워드 매칭보다 그 값이 우선한다(잘못된 값은 조용히 무시하고 기존 로직으로 폴백). 관리 화면은 따로 없음 — archive.json을 수동 편집 후 `python scripts/build_site.py` 재빌드.

## UI/UX 개선 4건 (2026-08-26)

사용자가 "UI/UX 개선"을 요청 → 직접 훑어본 뒤 4가지를 제안해 전부 진행:
1. **중복 통계 제거**: 헤더 우측 배지에 있던 "총 N주차·M건 수록"(`hbadge accent`)이 바로 아래 툴바의 `toolbar-info`와 완전히 같은 숫자를 반복 표시하고 있었음 — 헤더 배지 제거, 툴바 쪽만 유지.
2. **카드 종류별 시각적 차별화**: 표시사항 변경 추적(큐레이션 목록)/월간 통합 요약(AI 서술형)/주차 아코디언(원본 아카이브)이 전부 동일한 `.card` 스타일이라 헤더 텍스트를 안 읽으면 구분이 안 됐음. `.kpi` 카드의 좌측 강조선 패턴을 재사용해 `.label-spotlight`는 노랑(`--primary`), `.monthly-summary`는 파랑(`--info`, 신설 `.monthly-header` 클래스로 헤더 밑줄도 분리), `.week-section`은 중립(`--hairline`) 좌측 스트라이프를 부여 — 스크롤 중 색만으로 구분 가능.
3. **필터 3종 위치 통합**: 검색(툴바)·연도/상태(필터바)는 이미 인접해 있었지만, 카테고리 필터(`cat-overview`)는 KPI 카드 행 전체를 사이에 두고 그 아래에 있어 화면을 오가야 했음. `cat_overview_html`을 `kpi-row`보다 위로 재배치해 검색→연도/상태→카테고리가 한 흐름으로 이어지도록 함(카테고리 카드 자체의 마크업/JS는 안 건드림 — 순수 배치 변경이라 리스크 낮음).
4. **모바일 터치 타겟 확대**: `.filter-btn`(연도/상태 알약)이 `padding:3px 11px`(실측 높이 ~20px)로 권장 최소 터치 영역에 크게 못 미쳤던 것을 `padding:7px 14px`+`min-height:34px`로 확대, `filter-bar`의 버튼 간 `gap`도 6px→8px로 넓힘. `.btn-tool`(모두 펼치기 등)도 동일 기준으로 통일.

**검증**: 로컬 재빌드 후 grep으로 각 변경(헤더 배지 삭제, DOM 순서, CSS 규칙, 클래스명)이 실제로 반영됐는지 정적 확인 — 이 세션에 브라우저 자동화가 연결되지 않아 실제 클릭/시각 검증은 다음 세션(claude-in-chrome 연결 시) 권장.

## 데이터 소스 변경 이력 (2026-07-22)

사용자가 "식약처 법령은 법/시행령/행정예고/시행규칙에서, 식품 관련 내용만" 가져오자고 요청 → 실제 동작을 검증하던 중 세 가지 문제를 발견해 순차로 대응.

1. **식약처(`collect_mfds.py`) 게시판 번호가 애초에 틀렸음**: `BOARD_URL`이 `m_228`이었는데, 실제로 접속해보니 이건 "교육홍보물" 게시판이었다(법령과 무관). MFDS 사이트 내비게이션을 직접 뒤져서 진짜 법령 게시판을 찾음 — `m_203`("법, 시행령, 시행규칙" — 이미 공포된 법령), `m_209`("입법/행정예고" — 시행 전 예고). 두 게시판 모두 `?data_stts_gubun=C1002` 파라미터로 "식품" 분야만 서버사이드 필터링 가능(다른 코드: C1001 공통·C1003 의약품·C1004 의료기기·C1006 화장품 등 — MFDS가 식품 외 분야도 같은 게시판에서 다루기 때문에 이 필터가 핵심). 실제 목록 마크업도 `<table>`이 아니라 `<div class="bbs_list01"><ul><li>...` 구조라 셀렉터도 전면 교체. href가 상대경로("./view.do?...")라 문자열 접합 시 URL이 깨지던 버그(`mfds.go.kr./view.do`)도 `urljoin()`으로 수정.
2. **`law_type_utils.py` 신설**: `is_food_related()`(의약품/의료기기/화장품/마약류 등 명백히 무관한 키워드 배제), `detect_law_type()`(제목에서 법률/시행령/시행규칙/행정예고 판별) — collect_mfds.py/collect_foodsafety.py가 공유.
3. **식품안전나라(`collect_foodsafety.py`) 게시판도 무효화된 상태를 발견**: 기존 URL(`menu_no=2815`)이 "잘못된 접근입니다" 에러를 반환. 대체 게시판("법령 정보", menu_no=4805 등)을 찾아봤으나 최근 변경 공지 게시판이 아니라 현행 법령 전문을 보여주는 정적 참고자료 페이지였음 — 이 프로젝트가 필요로 하는 "최근 변경분 추적"과 성격이 다름. 식약처 게시판이 이미 필요한 내용을 다 커버하므로 **빈 리스트를 반환하도록 비활성화**만 하고 원인은 기록해둠.
4. **법제처(`collect_lawgokr.py`) 완전 제외**: `LAW_API_KEY`는 등록돼 있었지만 API가 계속 빈 응답("Expecting value: line 1 column 1")을 반환 — 키 만료 등 원인 불명. 사용자가 "법제처는 빼고 식약처만 진행, 웬만한 건 식약처에서 확인 가능"이라고 결정해 `collect_all.py`에서 호출 제거. `collect_lawgokr.py` 파일 자체와 `LAW_API_KEY` 시크릿은 남겨둠 — 키 문제가 나중에 해결되면 다시 넣을 수 있음.

**검증**: 로컬 실행 + 실제 GitHub Actions 워크플로 수동 트리거로 확인 — "건강기능식품에 관한 법률 시행규칙 일부개정령(안) 입법예고", "식품위생법 시행령 및 시행규칙 일부개정령 입법예고" 등 정확히 원하는 유형의 항목이 정상 수집됨.

**2026-07-22 후속**: `ANTHROPIC_API_KEY`를 등록해 한 번 테스트했으나 Anthropic 계정 크레딧 부족(과금 필요)으로 요약이 계속 실패 — 사용자가 "왜 요약에 API 비용이 필요하냐"고 질문, 무인 자동화(GitHub Actions)는 로그인 세션이 아니라 프로그램이 직접 호출 가능한 API가 필요하다는 점을 설명한 뒤, **무료 티어가 있는 Google Gemini API로 전환**하기로 결정. `summarize_items.py`를 `anthropic` SDK → `google-genai` SDK(`gemini-2.0-flash` 모델)로 전면 교체, 시크릿명도 `ANTHROPIC_API_KEY` → `GEMINI_API_KEY`로 변경(구 시크릿은 더 이상 사용 안 함, 삭제는 선택사항). API 키는 https://aistudio.google.com/apikey 에서 무료로 발급.

## 데이터 품질 주의사항

- `collect_historical.py`는 법제처 검색 결과 페이지를 정규식으로 파싱하는 1회성 백필 스크립트라 조문 텍스트 파편(예: "⑤ 제36조제1항...")이 법령명으로 잘못 추출될 수 있다. `is_article_fragment()` 가드로 걸러내지만, 재실행 후에는 결과를 육안으로 한번 검수할 것. (법제처 연동 자체는 2026-07-22 제외됐지만 과거 백필 데이터는 archive.json에 남아있음)

## archive.json 구조

```json
{
  "weeks": [
    {
      "year": 2026,
      "week_num": 26,
      "start_date": "2026-06-22",
      "end_date": "2026-06-28",
      "label": "2026년 26주차",
      "summary": "...",
      "counts": { "total": 5, "시행": 3, "예고": 2 },
      "collected_at": "2026-06-23T09:00:00",
      "items": [
        {
          "title": "건강기능식품에 관한 법률 시행규칙 일부개정",
          "source": "법제처",
          "status": "시행",
          "date": "2026-06-02",
          "url": "https://...",
          "key_points": ["내용 1", "내용 2"],
          "industry_impact": "...",
          "law_type": "시행규칙",
          "is_new": true
        }
      ]
    }
  ],
  "total_weeks": 1,
  "last_updated": "2026-06-23T09:00:00"
}
```

## GitHub 설정 가이드

### 1. 저장소 생성
- GitHub에서 `FOODLAW-MONITORING` 저장소 생성 (public 권장)
- 이 폴더 내용 전체를 push

### 2. GitHub Pages 활성화
- Settings → Pages → Source: `Deploy from a branch`
- Branch: `main` / Folder: `/docs`
- 저장 후 URL 확인: `https://primexx98-sudo.github.io/FOODLAW-MONITORING/`

### 3. API 키 등록 (GitHub Secret, 선택)
- Settings → Secrets and variables → Actions → New repository secret
- Name: `GROQ_API_KEY` / Value: Groq API 키 (https://console.groq.com/keys 에서 무료 발급, 미등록 시 요약 단계만 자동 건너뜀)
- `LAW_API_KEY`는 법제처 연동 제외(2026-07-22)로 더 이상 사용하지 않음

### 4. 첫 실행 (수동)
- Actions 탭 → `주간 식품 법령 수집` → `Run workflow`
- 약 2~3분 후 사이트 자동 업데이트 확인

## 수동 로컬 실행 방법

```bash
# 패키지 설치
pip install -r requirements.txt

# 수집 실행 (식약처만, 법제처 제외 상태)
python scripts/collect_all.py

# (선택) Groq API 요약 생성 — GROQ_API_KEY 필요(무료 발급)
set GROQ_API_KEY=발급받은키
python scripts/summarize_items.py

# 사이트 빌드
python scripts/build_site.py

# docs/index.html 브라우저로 열어서 확인
```

## 변경 이력

| 날짜 | 변경 내용 |
|---|---|
| 2026-08-27 | **UI/UX 디벨롭 6차**: ①"이전 주 접기" 버튼이 `:first-of-type` 스코프 오류로 사실상 동작 안 하던 버그 수정 ②월간 통합 요약 카드 최근 5개월만 노출+더보기 토글 ③표시사항 변경 추적(왼쪽)·주차 목록(오른쪽) 2단 컬럼 그리드 신설(`.content-grid`, 1080px 이하 단일 컬럼 폴백) + 왼쪽 컬럼 고정폭(320px)에서 항목 줄바꿈 깨지던 문제 수정. 로컬 Playwright 검증까지 완료, 커밋·push는 사용자 검토 후 진행 예정 |
| 2026-08-27 | **Gemini→Groq 전환**: 사용자가 "월간 통합 요약이 계속 2023년 9월 것만 보인다"고 지적 — 원인은 Gemini가 최근 며칠째 `503 UNAVAILABLE`(모델 과부하)을 반환 중인데 `call_gemini()`는 429만 재시도하고 503은 즉시 실패 처리해서, 매일 실행되는 백필에서 이번 달(2026-08) 포함 거의 모든 달이 빈 요약(`""`)으로 남는 구조였음(`gh run view --log`로 2026-08-26 11:41 실행 로그에서 503 연쇄 확인). 1차로 `call_gemini()`에 503도 최대 3회 지수백오프 재시도하도록 수정했으나, 수동 재트리거 테스트에서 재시도는 정상 작동했지만 그날 이미 Gemini 무료 티어 일일 한도(20회)를 소진해 결국 429로 실패 — 사용자 요청으로 Groq API(무료 티어, 일 1,000회)로 완전 교체 결정. `google-genai` SDK → `groq` SDK, 모델 `gemini-flash-latest` → `openai/gpt-oss-120b`(Groq가 단종시킨 `llama-3.3-70b-versatile`의 권장 후속 모델 중 한국어 벤치마크가 더 준수한 쪽), 시크릿 `GEMINI_API_KEY` → `GROQ_API_KEY`, 호출 간격 13초→5초, 백필 캡 `HISTORICAL_ITEM_CAP` 10→30·`DAILY_TOTAL_CAP` 18→120(무료 한도 여유 증가분 반영). `.github/workflows/collect.yml`·`backfill_summarize.yml`의 env 변수명도 함께 변경 |
| 2026-08-26 | **UI/UX 개선 4건**: ①헤더-툴바 중복 통계 배지 제거 ②표시사항 변경 추적/월간 통합 요약/주차 아코디언에 카드 종류별 좌측 강조선 색 분리(노랑/파랑/중립) ③카테고리 필터를 KPI 카드 위로 재배치해 검색·연도·상태·카테고리 필터를 한 흐름으로 통합 ④필터 알약 버튼(`filter-btn`)·툴바 버튼(`btn-tool`) 터치 타겟을 20px대→34px+로 확대 |
| 2026-08-26 | **구조 개선 4건**: ①연도 지연 로딩 — 최신 연도만 index.html에 인라인, 나머지는 docs/data/archive-weeks.html로 분리 후 검색/카테고리필터/과거연도선택/딥링크 시에만 클라이언트에서 fetch(`ensureAllYearsLoaded`) ②월간 통합 요약 카드 ↔ 주간 목록 연결(`jumpToMonth`, 주차별 `data-months` 속성) ③표시사항 변경 추적에 건별 업계영향 요약 라인 추가 ④`category_utils.py`에 `category_override` 필드 지원(개별 항목 수동 재분류) — food-monitor-hub 쪽 배포 지연 문제는 허브 저장소의 sync.yml 스케줄을 2시간 간격으로 단축하고, Claude가 소스 저장소 푸시 시 허브 동기화도 수동 트리거하는 습관으로 대응(별도 코드 변경 없음) |
| 2026-07-23 | **2023~2026년 과거 데이터 백필 + UI 연도 필터**: `collect_historical_mfds.py` 신설 — m_203(법/시행령/시행규칙)·m_209(입법/행정예고) 게시판을 `?page=N`으로 2023-01-01까지 페이지네이션하며 상세본문까지 함께 수집(m_203 약 5페이지·m_209 약 19페이지, 총 198건 신규 확보, 115주차 누적). 요약은 무료 티어 일일 호출 한도(실측 20회) 문제로 `summarize_items.py`를 재설계 — 당해년도 항목은 무제한, 그 이전 항목은 날짜 내림차순으로 하루 10건만 처리(`HISTORICAL_ITEM_CAP`), 항목+주간요약 합산 하루 18회로 안전마진(`DAILY_TOTAL_CAP`). 매일 자동으로 이 잔여분을 처리하는 `backfill_summarize.yml` 워크플로 신설(20:13 KST). UI는 `build_site.py`에 연도 필터 추가(기본값 최신 연도만 표시, 클릭으로 전체/과거 연도 전환) — 115주차를 전부 펼쳐서 보여주면 스크롤이 너무 길어지는 문제 해결. 헤더/필터에서 법제처·식품안전나라 배지 제거(현재 수집 출처가 식약처뿐이라 출처 필터 자체를 연도 필터로 교체) — 단, 과거에 이미 수집된 항목의 `source` 값 자체는 원본 그대로 보존 |
| 2026-07-23 | **호출 간격 13초로 조정**: `gemini-flash-latest`(내부적으로 `gemini-3.6-flash`)는 무료 티어가 분당 5회로 빡빡해서 기존 1초 간격으론 계속 429(RESOURCE_EXHAUSTED)에 걸림. `call_gemini()`에 429 감지 시 응답의 `retryDelay` 힌트만큼 대기 후 1회 재시도하는 로직 추가, 호출 간격도 13초로 상향(`CALL_INTERVAL_SEC`) |
| 2026-07-23 | **GEMINI_API_KEY 실제 등록 + 모델명 수정**: 설계서엔 등록됐다고 적혀 있었지만 실제 저장소 시크릿엔 없어서 요약이 매번 조용히 건너뛰어지고 있었음(발견 즉시 등록). 등록 후 `gemini-2.0-flash` 호출 시 무료 티어 할당량이 0인 채로 막혀있는 걸 발견 — 알고 보니 이 프로젝트에서 `gemini-2.0-flash`/`gemini-2.5-flash`/`gemini-1.5-flash` 전부 "신규 사용자에게 더 이상 제공되지 않는 모델"로 404 처리됨. `gemini-flash-latest`는 정상 응답 확인되어 모델명 교체(결제 등록 불필요) |
| 2026-07-23 | **요약 품질 개선(근거 기반 요약)**: 기존엔 Gemini/Claude에게 "제목만" 주고 개정 내용을 분석해달라고 시켜서, 모델이 그럴듯하지만 근거 없는 key_points·industry_impact를 지어내고 있었음(실측 확인, 8절 참고). `collect_mfds.py`에 `fetch_detail_body()` 추가 — 상세페이지(`.bv_cont`)의 실제 "개정이유 및 주요내용"·의견제출 마감일 등을 긁어와 `item["body_text"]`로 저장하고, `summarize_items.py` 프롬프트가 이 실제 본문을 근거로만 요약하도록 전면 수정(본문 없으면 "제목 기반 추정" 폴백, 결과에 명시). `related_laws`도 모델이 law.go.kr URL을 지어내던 걸 제거하고 본문에 실제 「」로 인용된 법령명만 텍스트 배지로 표시(`build_site.py` `.law-tag`). 기존 항목 백필용 `backfill_body_text.py` 신설 — 단, 2026-07-22 이전 구버전 수집기가 저장한 URL 중 일부는 상세페이지가 아니라 목록 페이지(`list.do`)를 그대로 저장한 깨진 링크라 백필 불가(이 항목들은 기존 제목 기반 요약 유지). `needs_update()`에 `summarized_with_body` 플래그 추가 — body_text가 새로 생기면 이미 요약된 항목도 자동 재생성 |
| 2026-07-22 | summarize_items.py를 Anthropic Claude API(유료 크레딧 필요, 계정 잔액 부족으로 실패)에서 Google Gemini API(무료 티어, `gemini-2.0-flash`)로 전환. 시크릿 `ANTHROPIC_API_KEY`→`GEMINI_API_KEY`, 패키지 `anthropic`→`google-genai` |
| 2026-07-22 | 식약처 게시판 번호 오류 수정(m_228→m_203/m_209), 식품 카테고리 서버필터 적용, law_type_utils.py로 법/시행령/시행규칙/행정예고 분류 정규화, 식품안전나라 게시판 무효화 확인 후 비활성화, 법제처 연동 완전 제외(API 빈 응답 + 식약처만으로 충분하다는 사용자 결정) |
| 2026-06-30 | 최초 설계 및 구현 |
| 2026-07-01 | 주차 정렬 버그 수정(최신 주차 최상단 고정), 과거 수집 데이터 조문 파편/중복 11건 정리(44→33건), 주간 자동화에 summarize_items.py(Claude API) 연동, UI에 "관심" 태그·"전체 아카이브 복사" 버튼 추가, collect_historical.py에 조문 파편 필터 추가 |
